# Private sale media

Keep high-resolution originals in `originals/` and customer-ready download files in `deliveries/` while preparing them for upload to Google Drive.

This directory is intentionally outside `src/assets`, so Angular does not publish it. It is also ignored by Git. For production, use the secured Google Drive folder configured by the API rather than hosting originals from this repository.
